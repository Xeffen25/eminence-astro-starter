{
	"name": "eminence-astro-starter",
	"type": "module",
	"version": "1.0.0",
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro build && astro preview",
		"astro": "astro",
		"generate-types": "wrangler types",
		"deploy": "astro build && wrangler deploy",
		"cf-typegen": "wrangler types",
		"format:check": "prettier . --check",
		"format:fix": "prettier . --write",
		"check": "pnpm format:check && astro check",
		"fix": "pnpm format:fix",
		"all": "pnpm format:fix && pnpm cf-typegen && astro check && pnpm build",
		"prepare": "husky"
	},
	"lint-staged": {
		"*": "prettier --write"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^13.1.2",
		"astro": "^6.0.7"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.8",
		"husky": "^9.1.7",
		"lint-staged": "^16.4.0",
		"prettier": "^3.8.1",
		"prettier-plugin-astro": "^0.14.1",
		"prettier-plugin-organize-imports": "^4.3.0",
		"typescript": "^5.9.3",
		"wrangler": "^4.75.0"
	}
}
